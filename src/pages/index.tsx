import {Container} from '@mantine/core';
import {HeroText} from "@/pages/compontents/HeroText";

export default function Index() {

    return (
        <Container>
            <HeroText/>
        </Container>
    );
}
